<Page x:Class="MyFlix.MediaDetailsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyFlix"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MediaDetailsView">

    <DockPanel LastChildFill="False" >

        <Image x:Name="Test" DockPanel.Dock="Left" Width="auto" Height="auto" Source="{Binding posterURL}"/>
        <DockPanel DockPanel.Dock="Top">
            <Button DockPanel.Dock="Left" Content="Return" Height="30" Width="100" Click="ReturnButtonClicked"/>
            <Label DockPanel.Dock="Right" Foreground="black" FontSize="23" Content="{Binding title}"/>
        </DockPanel>
        <TextBox Background="Transparent" Foreground="black" BorderThickness="0" Text="{Binding description}" TextWrapping="Wrap"/>
    </DockPanel>
</Page>
